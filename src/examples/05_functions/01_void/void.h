#include<iostream>

const int global = 10;

void use_local_variable();
void use_global_variable();

int show_area(double length = 20, double width=10);

double get_weekly_gross_pay(int hours, double rate);
double get_weekly_gross_pay(double yearly_salary);